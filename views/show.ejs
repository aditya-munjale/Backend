<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Details</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="w-full min-h-screen bg-gray-50 flex items-center justify-center">
    <div class="w-full max-w-3xl p-6 bg-white rounded-2xl shadow-lg">
      <a
        class="text-blue-500 hover:text-blue-600 underline"
        href="/"
        >⬅ Back to Home</a
      >

      <h1 class="text-3xl mt-5 font-semibold text-gray-900"><%= filename %></h1>
      <p class="text-lg p-3 mt-3 bg-gray-100 rounded-lg text-gray-700">
        <%= filedata %>
      </p>
    </div>
  </body>
</html>
