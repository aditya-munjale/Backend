<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Task</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="w-full min-h-screen bg-gray-50 flex items-center justify-center">
    <div class="w-full max-w-lg p-6 bg-white rounded-2xl shadow-lg">
      <a
        href="/"
        class="text-blue-500 hover:text-blue-600 underline mb-4 inline-block"
        >⬅ Back to Home</a
      >

      <h1 class="text-gray-900 text-2xl font-semibold mb-6">Edit Task Name</h1>

      <form action="/edit" method="post" class="flex flex-col gap-4">
        <input
          type="text"
          name="privious"
          value="<%= filename %>"
          readonly
          class="p-3 rounded-lg border border-gray-300 bg-gray-100 text-gray-500 focus:outline-none"
        />

        <input
          type="text"
          name="new"
          placeholder="Enter new file name"
          required
          class="p-3 rounded-lg border border-gray-300 text-gray-700 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-400"
        />

        <button
          type="submit"
          class="bg-blue-500 hover:bg-blue-600 transition text-white p-3 rounded-lg font-medium shadow"
        >
          💾 Save Changes
        </button>
      </form>
    </div>
  </body>
</html>
